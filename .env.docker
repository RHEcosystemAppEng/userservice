KEYCLOAK_BACKEND_URL="http://host.docker.internal:8080/"
ADMIN_USER="admin"
ADMIN_PASSWORD="admin"
GRANT_TYPE="password"
CLIENT_ID="admin-cli"
KEYCLOAK_REALM="master"
KEYCLOAK_MASTER_REALM_TOKEN=""
KEYCLOAK_TOKEN_PATH="realms/${KEYCLOAK_REALM}/protocol/openid-connect/token"
KEYCLOAK_GET_BY_USERNAME_PATH="admin/realms/${KEYCLOAK_REALM}/users?username="
KEYCLOAK_USERS_RESOURCE_URI="admin/realms/${KEYCLOAK_REALM}/users"
USER_SERVICE_PORT=":8000"
DISABLE_KEYCLOAK_CERT_VERIFICATION=false
